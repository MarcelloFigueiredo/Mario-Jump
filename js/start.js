document.getElementById("start-btn").addEventListener("click", function() {
    // Redireciona para a página do jogo (index.html)
    window.location.href = "../index.html";  // Corrige para navegar para o arquivo index.html
});
